import { MaximumCharacterPipe } from './maximum-character.pipe';

describe('MaximumCharacterPipe', () => {
  it('create an instance', () => {
    const pipe = new MaximumCharacterPipe();
    expect(pipe).toBeTruthy();
  });
});
